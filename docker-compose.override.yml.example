# Copy this file to docker-compose.override.yml for local overrides
# This file is automatically loaded by docker-compose and won't be committed

version: '3.8'

services:
  dev:
    environment:
      # Add your local environment variables here
      # - DATABASE_URL=postgresql://...
      # - API_KEY=your-key-here

